# #!/bin/sh

# set -e

# # Define the URL for the specified OpenCV version
# OPENCV_URL="https://github.com/opencv/opencv/releases/download/${OPENCV_VERSION}/opencv-${OPENCV_VERSION}.zip"

# # Download and unzip the OpenCV files
# curl -L ${OPENCV_URL} -o opencv.zip
# unzip opencv.zip -d /client/src/app/opencv

# # Remove the zip file
# rm opencv.zip
